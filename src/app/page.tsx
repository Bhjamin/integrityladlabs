import Nav from "./components/nav";

const Home = () => {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between bg-lightgray">
      <Nav />
    </main>
  );
};

export default Home;
